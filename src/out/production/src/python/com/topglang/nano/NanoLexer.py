# Keywords
keywords = 'pln public static int float'

# Numbers
numbers = '0123456789'

def __init__(self, text):
    self.text = text
    for text in keywords:
        for text in numbers:
            return '[topG] code = ' + keywords.__getitem__('int')

    for text in numbers:
        return '[topG] code = ' + keywords.__getitem__('int')